server {
    listen 80;
    server_name localhost;

    # Redirect all HTTP requests to HTTPS
    return 308 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name localhost;

	ssl_certificate /etc/nginx/ssl/selfsigned.crt;
    ssl_certificate_key /etc/nginx/ssl/selfsigned.key;
    ssl_dhparam /etc/nginx/ssl/dhparam.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';

    # Enable Gzip for compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # Protected Route: Verify Code Endpoint
    location /auth/validate-code {
        auth_request /auth/verify-token-user-code-service;
        auth_request_set $auth_status $upstream_status;

        error_page 401 = @error401;
        error_page 403 = @error403;

        proxy_pass http://ruby_user_management:4567/auth/validate-code;
        proxy_set_header Authorization $http_authorization;
    }

    # Protected SSR Code Endpoint
    location /validate-code {
        auth_request /auth/verify-token-user-code-service;
        auth_request_set $auth_status $upstream_status;

        error_page 401 = @error401;
        error_page 403 = @error403;

        proxy_pass http://ruby_ssr:4568/validate-code;
        proxy_set_header Authorization $http_authorization;
		#log
		error_log /var/log/nginx/auth_register_error.log debug;
    }

    location /profil {
        auth_request /auth/verify-token-user-code-service;
        auth_request_set $auth_status $upstream_status;

        error_page 401 = @error401;
        error_page 403 = @error403;

        proxy_pass http://ruby_ssr:4568/profil;
        proxy_set_header Authorization $http_authorization;
		#log
		error_log /var/log/nginx/auth_register_error.log debug;
    }

    # Root location for SSR
    location / {
        proxy_pass http://ruby_ssr:4568;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static files location
    location /static/ {
        proxy_pass http://ruby_ssr:4568;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Public Routes (42 Authentication)
    location /auth/logwith42 {
        proxy_pass http://ruby_user_management:4567/auth/logwith42;
    }

    location /auth/callback {
        proxy_pass http://ruby_user_management:4567/auth/callback;
    }

    location /auth/register {
        proxy_pass http://ruby_user_management:4567/auth/register;
		#log
		error_log /var/log/nginx/auth_register_error.log debug;
    }

    location /auth/login {
        proxy_pass http://ruby_user_management:4567/auth/login;
    }

    # Public Routes (Token Management)
    location /auth/refresh {
        proxy_pass http://ruby_user_management:4567/auth/refresh;
    }

    # Public Routes (Token Verification Endpoints)
    location /auth/verify-token-user {
        proxy_pass http://ruby_user_management:4567/auth/verify-token-user;
    }

    location /auth/verify-token-user-code {
        proxy_pass http://ruby_user_management:4567/auth/verify-token-user-code;
		#log
		error_log /var/log/nginx/auth_register_error.log debug;
    }

    location /auth/verify-token-admin {
        proxy_pass http://ruby_user_management:4567/auth/verify-token-admin;
    }

    location /user/me {
        auth_request /auth/verify-token-user-code-service;
        auth_request_set $auth_status $upstream_status;

        error_page 401 = @error401;
        error_page 403 = @error403;

        proxy_pass http://ruby_user_management:4567/user/me;
        proxy_set_header Authorization $http_authorization;
    }

    # Internal Token Verification Services
    location = /auth/verify-token-user-service {
        internal;
        proxy_pass http://ruby_user_management:4567/auth/verify-token-user;
        proxy_set_header Authorization $http_authorization;
    }

    location = /auth/verify-token-user-code-service {
        internal;
        proxy_pass http://ruby_user_management:4567/auth/verify-token-user-code;
        proxy_set_header Authorization $http_authorization;
    }

    location = /auth/verify-token-admin-service {
        internal;
        proxy_pass http://ruby_user_management:4567/auth/verify-token-admin;
        proxy_set_header Authorization $http_authorization;
    }

    # Protected Routes for Authenticated Users (e.g., /game/)
    location /game/ {
        auth_request /auth/verify-token-user-service;  # Use basic token check
        auth_request_set $auth_status $upstream_status;

        error_page 401 = @error401;
        error_page 403 = @error403;

        proxy_pass http://ruby_user_management:4567;
        proxy_set_header Authorization $http_authorization;
    }

    # Admin Protected Routes
    location /admin/ {
        auth_request /auth/verify-token-admin-service;
        auth_request_set $auth_status $upstream_status;

        error_page 401 = @error401;
        error_page 403 = @error403;

        proxy_pass http://ruby_user_management:4567;
        proxy_set_header Authorization $http_authorization;
    }

    # Error Handling
    location @error401 {
        return 401 'Unauthorized';
        add_header Content-Type text/plain;
    }

    location @error403 {
        return 403 'Forbidden';
        add_header Content-Type text/plain;
    }
}
